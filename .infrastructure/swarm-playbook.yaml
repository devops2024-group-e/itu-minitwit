- name: install docker
  hosts: swmmanagers:swmnodes
  become: yes
  roles:
   - apt
   - role: home-dir
     vars:
       directory: /minitwit-swarm
       sync_directory: ./swm-man-server/
   - role: docker
     vars:
       docker_conf: /minitwit-swarm
   - swarm
   - swarm-network
